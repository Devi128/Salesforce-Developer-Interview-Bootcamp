# Challenges – Day 10

- Understanding parent-to-child and child-to-parent communication was challenging at first.
- Implementing Custom Events and handling them in the parent component required careful understanding.
- Managing communication between sibling components through the parent was initially confusing.
- Connecting `MyApplications` with Apex and retrieving real `Application__c` records required debugging.
- Creating reusable components such as `StatusBadge` and `ApplicationCard` required understanding component responsibilities.
- Handling `refreshApex()` correctly to refresh the application list after applying for a job was challenging.
- Debugging the duplicate Eligible Jobs component in the Placement Home page was another challenge.
- Understanding the difference between client-side validation and server-side validation was important.
- Making sure the UI stayed updated after Salesforce data changed required careful testing.
- Keeping the LWC components focused and avoiding unnecessary coupling was challenging.
